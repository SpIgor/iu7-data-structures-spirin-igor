#include <stdio.h>
#include <stdlib.h>

#define LEN_ARRAY 4  // Длина массива стека
#define OK 0  // Код возврата при корректной работе подпрограмм
#define WRONG_ELEMENT -2  // Введен символ неподходящего типа
#define OVERFLOW -3  // Переполнение массива
#define EMPTY_ARRAY -5 // Пустой массив
